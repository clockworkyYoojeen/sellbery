const featuresRow=document.querySelector(".business__features-row");let leftEdge=featuresRow.getBoundingClientRect().left;const features=document.querySelectorAll(".business__features-row p"),toStart=document.querySelector("#slider-back");features[0].classList.add("inposition");const slides=document.querySelectorAll(".business__features-slide");for(let e of features)setLeftCoord(e),e.addEventListener("click",moveRow),e.addEventListener("click",moveSlide);function setLeftCoord(e){let t=e.getBoundingClientRect().left;e.dataset.initleft=t}function moveRow(e){const t=e.target,s=parseInt(t.dataset.initleft),o=(t.getBoundingClientRect().left,s-leftEdge+1);console.log(o),featuresRow.style.left=`-${o}px`,features.forEach(e=>e.classList.remove("inposition")),t.classList.add("inposition")}function moveSlide(e){const t=parseInt(e.target.dataset.index);for(let e=0;e<slides.length;e++)slides[e].classList.remove("show");slides[t].classList.add("show")}function moveToStart(e){if(e.stopPropagation(),"slider-back"===e.target.id){featuresRow.style.left=leftEdge+"px";for(let e=0;e<slides.length;e++)slides[e].classList.remove("show");slides[0].classList.add("show"),features[0].classList.add("inposition")}}toStart.addEventListener("click",moveToStart),window.addEventListener("resize",(function(){leftEdge=featuresRow.getBoundingClientRect().left;for(let e of features)setLeftCoord(e)}),!0),MicroModal.init({});const modalClose=document.querySelector("#modal-send");modalClose.addEventListener("click",(function(){const e=document.querySelector(".my-modal h2"),t=e.innerText;e.innerText="We will connect to you!",setTimeout(()=>{MicroModal.close("modal-1"),e.innerText=t},1200)}));